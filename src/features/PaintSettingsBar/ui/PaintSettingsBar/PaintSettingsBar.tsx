import { Toolbar } from "@/shared/ui/Toolbar";

export interface PaintSettingsBarProps {}

export const PaintSettingsBar = ({}: PaintSettingsBarProps) => {
  return <Toolbar className="top-toolbar"></Toolbar>;
};
